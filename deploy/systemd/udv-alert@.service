[Unit]
Description=Reporta a falha de %i

[Service]
Type=oneshot
User=ubuntu
WorkingDirectory=/opt/udv-shop
# O script escolhe o check pelo nome da unit: falha do host-check não pode marcar o check
# do backup como quebrado.
ExecStart=/opt/udv-shop/scripts/alert.sh %i
